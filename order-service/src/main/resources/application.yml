server:
  port: 9000
spring:
  apllcation:
    name: order-service

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        service: order-service
    sentinel:
      transport:
        dashboard: localhost：8021
  data
feign:
  sentinel:
    enabled: true